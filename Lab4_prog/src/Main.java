import Colors.Color;

public class Main {
    public static void main(String[] args) {
        Color.War war = new Color.War();
        System.out.println(war.action(Color.RED, Color.GREEN));

        //TODO
        // Literally: create every object you can and show every method and logic possible in your little pisslow project
    }
}